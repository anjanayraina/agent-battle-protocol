// src/Agent.js

class Agent {
    /**
     * Creates an Agent instance.
     * @param {string} name - The name of the agent.
     * @param {number} balance - The balance of the agent.
     */
    constructor(name, balance) {
        this.name = name;
        this.balance = balance;
    }
}

module.exports = Agent;
